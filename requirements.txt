telethon==1.36.0
fastapi==0.115.0
uvicorn==0.30.6
python-dotenv==1.0.1
anthropic>=0.39.0
aioapns==3.3.1

# Этап 2.4: dev-only — pytest backend integration tests (см. tests/).
pytest>=8.0
httpx>=0.27

# Этап 4.2: opt-in production monitoring (активируется при SENTRY_DSN env).
sentry-sdk[fastapi]>=2.0
